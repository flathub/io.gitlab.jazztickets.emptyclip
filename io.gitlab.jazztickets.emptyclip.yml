app-id: io.gitlab.jazztickets.emptyclip
runtime: org.freedesktop.Platform
runtime-version: '22.08'
sdk: org.freedesktop.Sdk
command: emptyclip
rename-desktop-file: emptyclip.desktop
rename-icon: emptyclip
rename-appdata-file: emptyclip.xml

finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio

modules:
  - name: emptyclip
    buildsystem: cmake-ninja
    builddir: true
    sources:
      - type: archive
        url: https://gitlab.com/jazztickets/uploads/-/raw/main/emptyclip-2.0.1-8955a4d-src.tar.gz
        sha256: 9cd00179baac611a3f0f028de69a4edc8683f8ffc1af21d85823759e3f2b06c3
